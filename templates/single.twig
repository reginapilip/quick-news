{% extends "base.twig" %}

{% block content %}
<div class="container ">
    <div class="row">
        <div class="col-4 bg-light pt-5">
            <h1>{{post.title}}</h1>
        </div>
        <div class="col-8 pt-5">
            <h2>By {{post.author}}</h2>
            <p class="fw-bold">{{post.date}}</p>
            <p>{{post.content}}</p>
            {#
            <pre>
                {{ dump(post) }}
            </pre> #}
        </div>
    </div>
    {#
</div> #}
{% endblock %}